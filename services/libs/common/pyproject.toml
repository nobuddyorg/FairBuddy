[project]
name = "common"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
  "aws-lambda-powertools",
]

[build-system]
requires = ["uv_build>=0.11.24,<0.12.0"]
build-backend = "uv_build"
